/**
 * Adapter: chapter 10 components use `useLang()` → { lang, toggle, setLang }.
 * The shared LanguageProvider is a superset, so we re-export it.
 */
export { useLang } from '../../../shared/providers/LanguageProvider';
