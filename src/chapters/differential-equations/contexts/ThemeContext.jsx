/**
 * Adapter over the shared ThemeProvider. Chapter 10 expects { theme, toggle }
 * and keys its CSS off `data-theme`, both supplied by the shared provider.
 */
export { useTheme } from '../../../shared/providers/ThemeProvider';
