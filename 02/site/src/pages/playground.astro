---
import Layout from '~/components/Layout.astro'
---

<Layout title="Playground — type any f(x) and try every method">
  <h1>Playground</h1>
  <p>
    Type a function $f(x)$ and a starting bracket / initial point, then run all five scalar methods —
    bisection, false position, Newton, secant, and fixed-point iteration on $g(x) = x - f(x)$ —
    side by side on the same problem.
  </p>

  <div class="rounded-md border border-dashed border-ink-300 dark:border-ink-700 p-6 my-6">
    <p class="font-medium">🚧 To be built</p>
    <p class="text-sm text-ink-600 dark:text-ink-400 mt-2">
      The Playground widget will reuse the same numerical kernels in <code>src/lib/methods/</code>
      and the math.js wrappers in <code>src/lib/expression.ts</code>.  Open the project plan at
      <code>/home/csd81/.claude/plans/based-on-these-3-sleepy-beaver.md</code> for the design.
    </p>
  </div>
</Layout>
