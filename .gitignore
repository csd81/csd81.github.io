node_modules/
dist/
*.tsbuildinfo
.vite/
.DS_Store
.env
.env.local

# Rust sandbox: keep the source, but never the 3.8GB build artifacts.
rust_app/target/
rust_app/crates/web/dist/
rust_app/**/*.pdf

# legacy per-chapter app build artifacts (kept as migration sources, not built)
0*/**/node_modules/
0*/**/dist/
10/**/node_modules/
10/**/dist/

# stray source scans / transcripts (not part of the site)
peldatar_pages/
linAlgPeldatar.md
tamop_37_53.md
tamop_pages/
la-tamop-wf*.md

# OCR source PDF (not bundled; Vite globs only *.md)
src/subjects/linalg/content/*.pdf
src/subjects/linalg/content/book/*.pdf
